<Page
    x:Class="TreasureHunt.App.Views.GameOver"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TreasureHunt.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Margin="10, 50, 10, 0" Grid.Row="0">
            <Image>
                <Image.Source>
                    <BitmapImage UriSource="ms-appx:///Assets/logo.png" />
                </Image.Source>
            </Image>
        </StackPanel>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center">
            <TextBlock Margin="0,20,0,0" FontWeight="Bold" FontSize="20">CONGRATULATIONS</TextBlock>
            <TextBlock x:Name="difficultyTextBlock" Margin="0,50,0,0">Difficulty:</TextBlock>
            <TextBlock x:Name="timeTextBlock" Margin="0,20,0,0">Time:</TextBlock>
        </StackPanel>

    </Grid>
</Page>
